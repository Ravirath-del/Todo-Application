apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment 
spec:
  replicas: 5
  selector:
    matchLabels:
      app: todoui
  strategy: 
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
  template:
    metadata:
      labels:
        app: todoui
    spec:
      containers:
      - name: todoapp
        image: pagal.azurecr.io/todoui:v4
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata: 
  name: service
spec:
  type: LoadBalancer
  selector:
    app: todoui
    
  ports: 
    - name: service-port
      port: 3600
      targetPort: 80

